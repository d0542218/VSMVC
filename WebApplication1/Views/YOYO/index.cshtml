@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*@using WebApplication1.Models;
@Model IEnumerable<lotteryUser>*@
    @{
        ViewBag.Title = "Movie list";
    }
    @section scripts{
        <script src="~/Scripts/angular.min.js" type="text/javascript"></script>
        <script src="@Url.Content("~/Models/lottery.js")" type="text/javascript"></script>

    }
    <div ng-app="demo">
        @*@foreach (lotteryUser user in Model)
        {
            <tr>
                <td>@user.ID</td>
                <td>@user.username</td>
                <td>@user.password</td>
            </tr>
        }*@
        <div ng-controller="demoController">
            <h2>{{Message}}</h2>
        </div>
        <div ng-controller="randomController">
            <span id="winner">{{randomNum}}</span>
            <br />
            <div id="startBT">
                <button ng-click="checkNum()">start</button>
                <button ng-click="stop()">stop</button>
            </div>
            <div id="inputs">
                <input id="min" ng-model="min" type="number" min="0" max="9999" />
                <input id="max" ng-model="max" type="number" min="10" max="15000" />
            </div>
        </div>
    </div>


